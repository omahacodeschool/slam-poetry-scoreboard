<div class="font-effect-anaglyph">Edit Slam<div>

<%= form_for(@slam) do |f| %>
  <% if @slam.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@slam.errors.count, "error") %> prohibited this slam from being saved:</h2>

      <ul>
      <% @slam.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :master_id %><br />
    <%= f.number_field :master_id %>
  </div>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :event_date %><br />
    <%= f.date_select :event_date %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <% @poets.each do |p| %>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<div class="bottom_link"><%= link_to 'Show', @slam %></div>
<div class="bottom_link"><%= link_to 'Back', slams_path %></div>
