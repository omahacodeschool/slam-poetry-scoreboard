<%= form_for @poet, :html => {:multipart => true} do |f| %>
  <% if @poet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@poet.errors.count, "error") %> prohibited this poet from being saved:</h2>

      <ul>
      <% @poet.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.file_field :photo %><br />
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
